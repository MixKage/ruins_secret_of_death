version: "3.9"

services:
  ruins-bot:
    build: .
    restart: unless-stopped
    environment:
      BOT_TOKEN: ${BOT_TOKEN}
      ADMIN_IDS: ${ADMIN_IDS}
      RUINS_DB_PATH: /data/ruins.db
    volumes:
      - ./ruins.db:/data/ruins.db
