version: "3.9"

services:
  ruins-bot:
    build: .
    restart: unless-stopped
    environment:
      BOT_TOKEN: ${BOT_TOKEN}
      ADMIN_IDS: ${ADMIN_IDS}
      DATABASE_URL: ${DATABASE_URL}
      BOT_TEST_MODE: ${BOT_TEST_MODE:-0}
